# Tasks: external-integrations-test-environments

> Sequencing: paperwork tasks (T01) start immediately — Logius/DSO/BRP grants have the longest
> lead times; code proceeds against the instant tiers (BRP docker mock, KvK open test key).

## Deduplication / Dependency Check

- [ ] **DC01**: Pin the research caveats: `docker pull ghcr.io/brp-api/personen-mock` to confirm the current tag; confirm the BRP proefomgeving key-request contact on the current getting-started page of `github.com/BRP-API/Haal-Centraal-BRP-bevragen`; confirm the KvK public test key is still published on developers.kvk.nl. Record the pinned values in design.md.
- [ ] **DC02**: Check `pluggable-integration-registry` status — if its registry landed, express adapter selection through it; otherwise implement the config-mode factory in `Application.php` and note the later migration in that change.
- [ ] **DC03**: Confirm `migrate-archival-to-or` progress — e-Depot tasks here (T10–T11) target OR's `Edepot/Transport/TransportInterface`, not procest's retired `EDepotSubmissionAdapterInterface`.
- [ ] **DC04**: Add the interlock note to `openspec/changes/brp-kvk-register-sets/proposal.md` (seed data = contract fixtures; live-adapter work lives here) — extend, don't duplicate.

## Access requests (start first — long lead times)

- [ ] **T01**: File the external access requests and record grant status in `docs/admin/integrations.md`: (a) BRP proefomgeving API key (e-mail to product owner per getting-started page); (b) DSO service request via the Ontwikkelaarsportaal/aansluitteam (client_id + test API key; ~5 working days per step) incl. PKIoverheid OIN/HRN certificate; (c) Logius DigiD preproductie application (supplier/Leverancier route — private-law orgs eligible) + PKIoverheid cert for the SAML koppelvlak (weeks); (d) Preservica Starter account (instant) for the e-Depot sandbox rehearsal.

## BRP (instant tier available)

- [ ] **T02**: Implement `lib/Service/External/Brp/HaalCentraalBrpAdapter.php` against the Haal Centraal BRP Personen bevragen API (X-API-KEY, configurable base URL) behind `integration.brp.mode`; Log adapter stays the default binding.
- [ ] **T03**: Add the BRP contract suite (`tests/integration/contracts/brp/`) running in PR CI against a `ghcr.io/brp-api/personen-mock` service container (offline); align its `test-data.json` personas with the `brp-kvk-register-sets` seed persons.

## KvK (instant tier available)

- [ ] **T04**: Implement `lib/Service/External/Kvk/KvkApiAdapter.php` against `https://api.kvk.nl/test/api` (public test key via config, `apikey` header) behind `integration.kvk.mode`.
- [ ] **T05**: Add the KvK contract suite pinned to the published fictitious companies (69599084, 68750110, 69599068, 55344526), `@group network`, network-gated CI lane (nightly/label), never in offline PR CI.

## DSO / Omgevingswet

- [ ] **T06**: Extend DSO config (`integration.dso.baseUrl` + existing `dso_lv_auth_token` + client cert wiring) to target `service.pre.omgevingswet.overheid.nl`; `DsoLvAuthService` keeps warn-and-empty-headers when unconfigured.
- [ ] **T07**: On grant: record pre-prod request/response fixtures for the vergunningaanvraag intake flow; contract-test `DsoIntakeService` against the recorded fixtures in CI; document the manual live pre-prod smoke runbook.

## DigiD / eHerkenning

- [ ] **T08**: Implement `SimulatorDigidSamlAdapter` + `SimulatorEHerkenningSamlAdapter` (maykinmedia mock-login pattern: BSN/KVK entry form, visible "simulatie" labelling, session marked simulator-authenticated) selectable via `integration.digid.mode=simulator`; Playwright e2e for the simulator login journey.
- [ ] **T09**: Implement the real SAML-artifact adapters and validate against Logius preproductie once the cert + aansluiting are granted; document the koppelvlak checklist conformance in `docs/admin/`.

## e-Depot

- [ ] **T10**: Add the CI MDTO-validation step: every SIP generated by the (OR-side) pipeline validates against `NationaalArchief/MDTO-XSD` (mdto.py or xmllint), offline.
- [ ] **T11**: Configure OR's RestApi/Sftp transport against the Preservica Starter sandbox and run one manual end-to-end ingest rehearsal; record the runbook + results; note the NA e-Depot aansluittraject as customer-side (zorgdrager) in the docs.

## Promotion

- [ ] **T12**: Add per-integration feature entries to `openspec/features.overlay.json` graded by the promotion criteria (start: BRP/KvK `beta` once contract lanes are green; DSO/DigiD/e-Depot `mock`/`beta` per evidence); simulator-DigiD capped at `beta` permanently.

## Verification Tasks

- [ ] **V01**: Fresh install: zero external requests on any seam (network capture while exercising initiator search, DSO intake, login, archival) — Log adapters everywhere.
- [ ] **V02**: BRP: PR CI green offline against personen-mock; flipping `integration.brp.mode=test` on a dev instance returns a proefomgeving persona through the initiator-search UI.
- [ ] **V03**: KvK: network lane green against api.kvk.nl/test; the four pinned fictitious companies resolve through the UI search.
- [ ] **V04**: DigiD simulator: e2e login journey passes; UI shows the simulation label; session is marked simulator-authenticated.
- [ ] **V05**: e-Depot: an intentionally-broken MDTO field fails the CI validation step; the Preservica Starter rehearsal produced an ingest confirmation recorded in the runbook.
- [ ] **V06**: Overlay statuses match the evidence trail (each promotion links its green lane / official-environment proof).
