# Tasks

- [ ] `StoreDescriptor` for `case-type-template`, and a thin `search()` over the engine client
- [ ] `install()` writing through `ConfiguredRegistryService`, refusing record schemas
- [ ] Registry settings with a write-only token
- [ ] The Store page and the footer menu entry
- [ ] Analysis stubs, so the injected engine types resolve without OpenRegister
- [ ] Tests: the allowlist refusal, the write-only token, and the offline fallback
