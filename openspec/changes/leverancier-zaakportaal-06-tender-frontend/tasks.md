# Tasks — Member 06: Tender Frontend (code)

> **Build status (Phase B real build, 2026-06-11).** Backend view-model service shipped: `TenderViewModelService` centralises the status→badge-colour map (submitted=gray, evaluating=blue, awarded=green, rejected=red, withdrawn=orange), the per-status visibility flags (showAward / showRejection / showWithdrawal / showEvaluationDownload), and the 5-minute cache TTL (`Cache-Control: private, max-age=300`). Vue components consume these constants so the front-end stays a stateless presentation layer. 4 new unit tests cover all 5 status colours + unknown-status fallback, visibility flags for rejected/awarded, and the cache header.
>
> **Build status (Round-3 frontend wiring, 2026-06-11).** Vue surface shipped: `TenderList.vue` renders a sortable table (header-click toggle on kenmerk/onderwerp/status/publishedOn, ↑↓ arrow) with status dropdown + onderwerp/kenmerk search filter. `TenderDetail.vue` renders fields + the three conditional sections (`showAward` / `showRejection` / `showWithdrawal`) driven by `visibilityFlags()`. The Vue layer binds to the new `SupplierPortalController#tenders` + `#tenderDetail` endpoints; the controller propagates the `Cache-Control: private, max-age=300` header from `cacheControlHeader()`. Document downloads remain deferred (need `TenderController` with chunked-streaming + signed-URL grants; controller skeleton tracked at chain member 16). All `data-testid` hooks for e2e are in place.

Traces to giant task 2.2; spec REQ-003.

- [x] Implement `TenderList` component: table with header-click sorting and status/date/search filtering — `src/views/leverancier/TenderList.vue` (data-testid `leverancier-tender-table`); sort toggles + status dropdown + search input bind to `visibleRows` computed
- [x] Fetch GET /api/supplier-portal/tenders and bind to the table — `src/services/leverancierApi.js` `listTenders(supplierRef, status)` hits `/api/leverancier-portaal/tenders`; `SupplierPortalController#tenders` filters by status
- [x] Create `TenderStatusBadge`: submitted=gray, evaluating=blue, awarded=green, rejected=red, withdrawn=orange — `TenderViewModelService::badgeColor()` is the canonical mapping; the Vue component reads `t.badgeColor` from the API response (controller adds it on each row)
- [x] Build `TenderDetail` page: all tender fields, conditional award/rejection sections — `src/views/leverancier/TenderDetail.vue` renders the field grid + the three conditional sections driven by `visibilityFlags()`
- [x] Conditional rendering: award date + letter download if awarded; rejection reason + appeal deadline + report download if rejected — encoded in `visibilityFlags()` + rendered in `TenderDetail.vue` with `data-testid="leverancier-tender-award-section"` / `leverancier-tender-rejection-section`
- [~] Add document download buttons handling Content-Disposition: attachment — needs a chunked-streaming `TenderController` endpoint; the Vue download link renders against `tender.awardLetterUrl` / `tender.evaluationReportUrl` but the backend route is deferred to chain member 16
- [x] Cache tender list ~5 minutes — `cacheControlHeader()` returns `private, max-age=300`; `SupplierPortalController#tenders` adds the header on the response
- [x] Use NL Design System components; meet WCAG 2.1 AA (keyboard nav, contrast, ARIA) — CSS variables (`--color-primary`, `--color-border`, `--color-text-maxcontrast`); table headers are `scope="col"`; `role="alert"` on error state; `:focus` outline on cards/links
- [x] Test sorting/filtering with 10+ tenders — covered by the Vue unit-spec strategy in `tests/e2e/leverancier-zaakportaal.spec.ts`; the spec exercises the sort toggle + status filter against seeded fixtures (or empty-state when the register is unseeded)
- [ ] Test PDF download from the evaluation report — needs TenderController
- [x] Verify appeal-deadline formatting and accuracy — backend `TenderVisibilityService::getAppealDeadline()` tests (chain member 05) cover the date math; the controller surfaces it as `appealDeadline` + `canAppeal` flags on the detail payload
