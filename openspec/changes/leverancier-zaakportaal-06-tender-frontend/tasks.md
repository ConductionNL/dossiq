# Tasks — Member 06: Tender Frontend (code)

Traces to giant task 2.2; spec REQ-003.

- [~] Implement `TenderList` component: table with header-click sorting and status/date/search filtering — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Fetch GET /api/supplier-portal/tenders and bind to the table — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Create `TenderStatusBadge`: submitted=gray, evaluating=blue, awarded=green, rejected=red, withdrawn=orange — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Build `TenderDetail` page: all tender fields, conditional award/rejection sections — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Conditional rendering: award date + letter download if awarded; rejection reason + appeal deadline + report download if rejected — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Add document download buttons handling Content-Disposition: attachment — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Cache tender list ~5 minutes — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Use NL Design System components; meet WCAG 2.1 AA (keyboard nav, contrast, ARIA) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Test sorting/filtering with 10+ tenders — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Test PDF download from the evaluation report — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Verify appeal-deadline formatting and accuracy — deferred to downstream cycle / fleet-wide adoption (handoff)
