# Tasks — Member 16: Cross-Cutting Tests, A11y/Security Audit & Docs (code)

Traces to giant tasks 5.1–5.6, 6.3, 6.4.

- [~] Write E2E: login via eHerkenning → dashboard → view tender → download report → logout — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Write E2E: admin invites member → member activates → views role-scoped tabs — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Write E2E: view invoice → send message → receive response — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Write E2E: contract renewal request workflow — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Write E2E: update address (immediate) and IBAN change (creates Procest zaak) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Run cross-cutting integration tests asserting scope isolation (supplier B → 403 on supplier A) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Run rate-limit (429 on 101st) and audit-log (masked PII) integration assertions — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Run automated accessibility audit (Axe/Lighthouse) + manual keyboard + screen-reader pass — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Verify contrast ≥4.5:1 across all pages — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Run security audit: XSS/CSRF/injection scan + manual review; attempt scope/CORS/rate-limit bypass — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Write API docs (endpoints, request/response schemas, examples) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Write deployment guide (env vars, dependency service URLs, repair-step setup) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Write user guide + troubleshooting (eHerkenning, KvK, payment-date) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Create release checklist, staged rollout, rollback, and communication plan — deferred to downstream cycle / fleet-wide adoption (handoff)
