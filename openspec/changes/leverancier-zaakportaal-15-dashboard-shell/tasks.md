# Tasks — Member 15: Dashboard Shell & Layout (code)

Traces to giant task 2.1; spec REQ-003/004/005/008 summary surfaces.

- [~] Create `PortalLayout` component: header (logo, user menu, nav), main content area, footer — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Build `DashboardSummary`: 4 cards (tenders, invoices, contracts, KPI) with counts and badges — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Implement `NavBar`: dynamically show tabs by role (reuse member 03 role→tab matrix) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Add user profile menu: name, role, "Mijn Gegevens" link, Logout button — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Link each summary card into its corresponding feature view — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Implement responsive design (CSS-grid breakpoints for mobile/tablet) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Add loading states and error boundaries — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Use NL Design System components (buttons, cards, tables, modals) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Test with a screen reader (NVDA) for accessibility — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Test color contrast (≥4.5:1 normal text) and full keyboard navigation — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Verify logout destroys session and redirects to login — deferred to downstream cycle / fleet-wide adoption (handoff)
