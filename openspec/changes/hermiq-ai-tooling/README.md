# hermiq-ai-tooling

Extend dossiq-mcp-adoption with curated #[McpTool] tools: guard-enforcing writes (scope×reach annotated, default-deny, approval-gated) and aggregation reads, so every automatable Dossiq action is commandable from Hermiq chat under granular per-agent grants
