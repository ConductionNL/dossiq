# Tasks — Member 14: KPI Frontend (code)

Traces to giant task 2.7; spec REQ-008-A/B/C.

- [~] Implement `KPICard` component: title, current value, benchmark comparison, embedded trend — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Fetch GET /api/supplier-portal/kpis (snapshot) and /kpis/trends (12-month) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Create `TrendChart`: line for payment days + on-time %, bar for dispute rate — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] X-axis month labels; metric-specific Y-axis; hover tooltip ("May 2026: 28 days") — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Skip insufficient-data months from the chart with an "Onvoldoende gegevens" label — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Implement benchmark comparison indicators (own vs municipal average) — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Implement CSV export button: GET /kpis/export, trigger download — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Use NL Design System components; meet WCAG 2.1 AA — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Test with 12 full months of data — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Test sparse-data months — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Verify benchmark comparison rendering — deferred to downstream cycle / fleet-wide adoption (handoff)
