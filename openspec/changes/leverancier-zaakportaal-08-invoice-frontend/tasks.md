# Tasks — Member 08: Invoice Frontend (code)

Traces to giant task 2.3; spec REQ-004.

- [~] Implement `InvoiceList` component: sortable/filterable table with status badges — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Fetch GET /api/supplier-portal/invoices with status/date/amount filters — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Create status badges: received, under_review, approved, disputed, rejected, paid — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Build `InvoiceDetail` page: expected payment date in green box if approved; actual + delta if paid — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Implement `AgeAnalysisBar`: stacked bar with 0–30/30–60/60–90/90+ buckets — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Bucket filtering: clicking a bucket filters the list to that age range — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Dispute entry: if status=disputed, show "Reactie geven" opening the message composer — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Red badge on 90+ day overdue invoices in the list — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Use NL Design System components; meet WCAG 2.1 AA — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Test age buckets with boundary edge cases — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Test forecast display states (approved/paid/under_review/disputed) — deferred to downstream cycle / fleet-wide adoption (handoff)
