# Tasks: field-rules-declared

Tier: V1. Kind: config. Row 13.8.

- [ ] 1.1 `lib/Settings/dossiq_register.json` `case`: the five rules (D-1);
  repair step for the two groups.
  - `@spec openspec/changes/field-rules-declared/specs/security-hardening/spec.md`
- [ ] 1.2 vitest: no role branch on these fields under `src/` (D-2).
- [ ] 2.1 `tests/e2e/field-rules.spec.ts`; `openspec validate
  field-rules-declared --strict`.
