80 citations are not verified, across 32 of the suite's 55 cited files.

### `tests/e2e/case-type-edit-and-setup.spec.ts`

11 of 15 citations: 9 partial, 2 smoke.

- **partial** `no anchor, cites openspec/specs/first-time-setup/spec.md`  _safety, credits nothing_  
  the wizard offers no step the seed action cannot fulfil  
  Anti-coverage worth acting on: the anchorless citation is to a spec whose REQ-SETUP-PRO-001 says a seed step SHALL be declared and REQ-SETUP-PRO-002 says the seed action SHALL succeed, and this test asserts the inverse, that seed is absent from the steps and that the action answers 422 with success false; it does prove register-check is still reported, so the spec is stale rather than the test wrong.

- **partial** `no anchor, cites openspec/specs/first-time-setup/spec.md`  _credits nothing_  
  the tour a user actually gets includes the Case types and Flows stops  
  The citation names no requirement, and the first-time-setup spec says nothing about the walkthrough tour: the test greps the served bundle for the see-case-types and see-flows step ids, which no setup requirement mentions.

- **partial** `no anchor, cites openspec/specs/first-time-setup/spec.md`  _credits nothing_  
  the tour opens for a user who has not seen it, and offers all seven steps  
  Anchorless, and the tour is not the setup wizard the spec describes: asserting the walkthrough dialog shows seven steps, or that a seen-version explains its absence, survives any breakage of the manifest setup block, the seed action or the status endpoint.

- **partial** `no anchor, cites openspec/specs/first-time-setup/spec.md`  _credits nothing_  
  neither new stop forces the user to create anything  
  Anchorless citation onto a spec that covers the setup wizard only; the test reads the bundle to prove the two walkthrough stops allow a manual next and do not require object-created, which no first-time-setup requirement states.

- **partial** `no anchor, cites openspec/specs/first-time-setup/spec.md`  _credits nothing_  
  both new stops can actually anchor to their nav item  
  Anchorless, and about the walkthrough rather than the wizard: it asserts the nav renders a data-cn-route anchor for CaseTypes and Flows, a real and useful check but one no first-time-setup requirement covers.

- **partial** `#TASK-CT-13`  _credits nothing_  
  Edit navigates to the case type detail route  
  Lands on the case-type detail route where the seven tabs live but asserts only the URL shape, so removing or breaking the Properties, Docs and Decisions tabs the cited task covers leaves every assertion green. Was `dangling` on 2026-09-11.

- **partial** `#TASK-CT-13`  _credits nothing_  
  Edit does not open a dialog over the index  
  Drives the detail route and asserts the absence of an Edit Case Type dialog, which is the modal-versus-page defect and not the seven-tab integration TASK-CT-13 states. Was `dangling` on 2026-09-11.

- **partial** `#TASK-CT-13`  _credits nothing_  
  the detail page offers an Edit button  
  Opens the case-type detail page the cited task is about and asserts the edit button is visible, never that any of the seven tabs render or fetch their sub-entities. Was `dangling` on 2026-09-11.

- **partial** `#TASK-CT-13`  _credits nothing_  
  the Edit button opens the record form  
  Opens the record form from the detail page and asserts a dialog appears; the tabs TASK-CT-13 asks a smoke run to verify are never touched, so the citation claims coverage the body does not give. Was `dangling` on 2026-09-11.

- **smoke** `#TASK-CT-13`  _credits nothing_  
  every entry in the row menu has a visible label  
  The anchor is the deferred TASK-CT-13 smoke-test task for the seven-tab case-type detail integration; this test never leaves the case-types index and only asserts that no row-menu entry renders with an empty label, so breaking the tabs leaves it green. Was `dangling` on 2026-09-11.

- **smoke** `#TASK-CT-13`  _credits nothing_  
  the menu offers View and Edit exactly once each  
  Same deferred seven-tab task; the test counts the words View and Edit in the index row menu text and asserts nothing about the Properties, Docs or Decisions tabs the task names. Was `dangling` on 2026-09-11.


### `tests/e2e/spec-coverage/deelzaak-support.spec.ts`

8 of 18 citations: 7 partial, 1 smoke.

- **partial** `#delete-case-without-sub-cases-proceeds-normally`  _safety_  
  the sub-cases page warns about orphans for a parent with sub-cases  
  This test seeds a parent WITH a sub-case and only ever exercises the orphan branch, so nothing in it says what a childless case's delete dialog looks like; breaking the plain confirmation would leave every assertion here green, and the honest citation for this scenario is the sibling test at line 400. Not in the 2026-09-11 population.

- **partial** `#parent-case-shows-sub-cases-list`  
  the Sub-cases tab renders either a list or an empty state without error  
  The helper does assert the sub-cases section renders something, but the test's own assertion is expect(hasTable || hasEmpty).toBeTruthy(), which an empty state satisfies, and it opens whatever case the register hands back first, so the scenario's three sub-cases with title, status, assignee, deadline and clickable links are never read.

- **partial** `#parent-case-with-no-sub-cases-shows-empty-state`  
  the Sub-cases tab renders either a list or an empty state without error  
  Same or-disjunction: hasTable satisfies it, so the empty-state sentence the scenario names need never render, and the Create Sub-case button the scenario also requires is not asserted at all; the fixture is an arbitrary existing case that may well have sub-cases.

- **partial** `#roll-up-shows-completion-progress`  
  opening a sub-case shows the parent breadcrumb and the list shows a completion roll-up  
  The roll-up assertion is guarded by if ((await rollup.count()) > 0), so a header that prints no roll-up passes, and even inside the guard it matches any X/Y rather than the counts the seeded fixture would imply; no sub-case is given an endDate anywhere in the test.

- **partial** `#roll-up-with-no-completed-sub-cases`  
  opening a sub-case shows the parent breadcrumb and the list shows a completion roll-up  
  Same guarded roll-up locator and the same any-X-over-Y pattern; the scenario's specific 0/2 case is never seeded and the zero-completed reading is never distinguished from the completed one.

- **partial** `#sub-case-counts-batch-loaded-per-page`  
  the case list shows a sub-case badge, and only on a parent  
  Counts requests to /api/deelzaken/counts and allows up to two, but the list is narrowed by title to a single seeded row, so a per-row N+1 implementation also makes one request and passes; widen the fixture to several parents on one page to make the batching claim falsifiable. Not in the 2026-09-11 population.

- **partial** `#sub-case-shows-parent-breadcrumb`  
  opening a sub-case shows the parent breadcrumb and the list shows a completion roll-up  
  The breadcrumb assertion sits inside if ((await breadcrumb.count()) > 0) with an else branch that writes an annotation and passes, and the row click is wrapped in .catch(() => {}), so a case detail that renders no parent breadcrumb at all reports green.

- **smoke** `#top-level-case-has-no-breadcrumb`  
  opening a sub-case shows the parent breadcrumb and the list shows a completion roll-up  
  Nothing in this test opens a case with a null parentCase or asserts the absence of a breadcrumb on one; the only unconditional assertions it leaves for this scenario are that the section rendered and that the body does not read Internal Server Error.


### `tests/e2e/spec-coverage/integrations-and-flows.spec.ts`

8 of 9 citations: 7 dangling, 1 partial.

- **dangling** `#scenario-flows-are-authored-in-the-app`  _credits nothing_  
  the settings menu has a Flows entry pointing at an in-app route  
  Points at openspec/specs/automatic-actions-surface/spec.md, which does not exist; the scenario Flows are authored in the app lives at openspec/changes/page-topology-cleanup/specs/automatic-actions-surface/spec.md line 83, and the nearest published capability is automatic-actions.

- **dangling** `#scenario-flows-are-authored-in-the-app`  _credits nothing_  
  /flows renders this app's own flow surface  
  Same missing automatic-actions-surface path; the anchor resolves only inside the unarchived change, so this citation credits nothing until page-topology-cleanup is archived.

- **dangling** `#scenario-flows-are-authored-in-the-app`  _credits nothing_  
  /flows/new renders the flow canvas  
  Same missing automatic-actions-surface path, third citation of the same absent file on this test file.

- **dangling** `#scenario-no-duplicate-configuration-entries`  _credits nothing_  
  exactly one entry links to the administration surface  
  Points at openspec/specs/admin-settings-surface/spec.md, which does not exist; the scenario No duplicate configuration entries lives at openspec/changes/page-topology-cleanup/specs/admin-settings-surface/spec.md line 31, and the nearest published capability is admin-settings.

- **dangling** `#scenario-procest-hosts-no-ai-oversight-pages`  _credits nothing_  
  no sidebar link points into another app  
  Points at openspec/specs/ai-oversight-surface/spec.md, which does not exist; the scenario Procest hosts no AI-oversight pages lives at openspec/changes/page-topology-cleanup/specs/ai-oversight-surface/spec.md line 16, and the nearest published capability is ai-oversight-log.

- **dangling** `#scenario-procest-hosts-no-processing-activities-page`  _credits nothing_  
  no sidebar link points into another app  
  Points at openspec/specs/avg-processing-surface/spec.md, which does not exist; the identical anchor does resolve inside the open change at openspec/changes/page-topology-cleanup/specs/avg-processing-surface/spec.md, and the nearest published capability is avg-verwerkingenlogging.

- **dangling** `#scenario-procest-hosts-no-processing-activities-page`  _credits nothing_  
  the AVG and AI-oversight entries are gone from the navigation  
  Same missing path as the other avg-processing-surface citation on this file; the anchor resolves only inside the unarchived page-topology-cleanup change.

- **partial** `no anchor, cites openspec/specs/case-types/spec.md`  _credits nothing_  
  the case types index renders its own create control  
  The citation names a whole spec file and therefore no requirement; worse, the create control is matched by /Add|New|Create/i, which the Dashboard's own New case button satisfies, so the route fallthrough this test exists to catch leaves it green.


### `tests/e2e/spec-coverage/document-zaakdossier.spec.ts`

6 of 9 citations: 6 smoke.

- **smoke** `#req-zak-004a-dossier-groups-documents-by-type-with-count-badge`  
  cases index renders so a case dossier tab can be opened  
  Asserts only that /cases does not print Internal Server Error, and never opens a dossier tab, so the type grouping and the Dossier (8) count badge the requirement specifies go unobserved.

- **smoke** `#req-zak-004b-empty-dossier-shows-upload-cta-with-drag-and-drop-zone`  
  cases index renders so a case dossier tab can be opened  
  Same body as the row above: a 500-string check on the cases index, with no empty dossier opened and no assertion on the upload call to action or the drag and drop zone.

- **smoke** `#req-zak-004c-sort-and-filter-controls-work-per-column`  _never runs, credits nothing_  
  dossier sort and per-file progress are not observable here, blocked by #764  
  The body opens with an unconditional test.fixme(true, ...), so nothing runs and the citation claims per-column sort and filter coverage that is never executed; the fixme reason states this plainly. Not in the 2026-09-11 population.

- **smoke** `#req-zak-005b-per-file-upload-progress-with-shared-metadata`  _never runs, credits nothing_  
  dossier sort and per-file progress are not observable here, blocked by #764  
  Unconditional test.fixme(true, ...): per-file upload progress is never observed, and the only statement after it is a 500-string check on the cases index. Not in the 2026-09-11 population.

- **smoke** `#req-zak-006a-concept-document-version-history-shows-restore`  _never runs, credits nothing_  
  version history needs a multi-version document, blocked by #764  
  Unconditional test.fixme(true, ...) because no seeded document carries a second Nextcloud file version, so no version list and no restore control is ever asserted. Not in the 2026-09-11 population.

- **smoke** `#req-zak-008a-zip-export-includes-manifestcsv-and-type-sub-folders`  _never runs, credits nothing_  
  ZIP export contents are not asserted in the browser, blocked by #764  
  Unconditional test.fixme(true, ...): the ZIP is never downloaded or unpacked, so manifest.csv and the per-type sub-folders are asserted only in Newman and ZipManifestBuilderTest, not here. Not in the 2026-09-11 population.


### `tests/e2e/spec-coverage/pdok-via-openconnector.spec.ts`

4 of 4 citations: 2 smoke, 1 dangling, 1 partial.

- **dangling** `#scenario-suggest-call-reaches-openconnector-instead-of-api-pdok-nl`  _credits nothing_  
  suggest reaches the openconnector endpoint, never api.pdok.nl  
  The anchor spells the host with hyphens, scenario-suggest-call-reaches-openconnector-instead-of-api-pdok-nl, while the heading at line 27 slugs its dots away to scenario-suggest-call-reaches-openconnector-instead-of-apipdoknl, so the citation resolves to nothing; the near miss is that one heading, and the test body under it is a strong routing assertion that currently credits nobody.

- **partial** `#scenario-503-response-resolves-with-null-and-surfaces-message_key`  _credits nothing_  
  503 from openconnector degrades gracefully without throwing  
  Opens the dossiq app with its service worker in control, which is the surface the shim runs on, but never calls the shim: the body fetches /apps/openconnector/api/pdok/lookup directly and then asserts the 503 and the message_key its own route handler just returned, so a pdokService.lookup() that threw instead of resolving with null would leave every assertion green. Anchor note for the repair: the heading exists at openspec/specs/pdok-consumer/spec.md:89 but gate-19 does not slug it as a scenario, so this citation credits nothing until the anchor is respelled.

- **smoke** `#scenario-all-six-functions-are-exported-with-unchanged-signatures`  
  seeded OR addresses are retrievable without PDOK/openconnector being available  
  Seeds two OpenRegister address rows and asserts they come back, which says nothing about the six exported shim functions or their signatures, and the whole body skips when the addresses register is absent.

- **smoke** `#scenario-openconnector-absent-surfaces-warning-without-blocking-form`  
  404 (openconnector absent) does not block the page  
  Asserts its own mocked 404 is echoed back and that the page is not the login screen; the scenario's actual claims, an inline warning on the address field and a form that still submits, are never asserted, and the call bypasses the shim by fetching the endpoint directly.


### `tests/e2e/pages.spec.ts`

3 of 7 citations: 2 partial, 1 smoke.

- **partial** `#scenario-card-and-table-view`  _spec says exclude_  
  renders as a card index scoped to the current user  
  Asserts the Urgency and Newest sort controls and that a Cards button is visible, but it never switches to Table, never reads the identifier, title, case type, status and deadline columns the scenario names and never asserts card view is the default; note the requirement above the scenario carries an @e2e exclude for being data dependent, which this citation contradicts.

- **partial** `#view-the-global-task-list`  
  renders list view with search and filters, and offers no Add  
  Drives /tasks and asserts the view switcher, that no Add button exists and that the search field is attached, but it never reads a single row, so the scenario's paginated list whose rows show title, parent case reference, status, assignee, due date and priority survives untouched; an empty list passes.

- **smoke** `#in-app-settings-page-renders-configuration-sections`  
  renders the configuration section and its save control  
  The in-app /settings page the requirement names was retired, so the test drives /settings/admin/dossiq instead and asserts only that one exact Save button is visible and the body holds no Internal Server Error; none of the three section headings the scenario lists, Version Information, Configuration and Case Type Management, is asserted. Was `partial` on 2026-09-11.


### `tests/e2e/spec-coverage/handler-vervanging-waarneming.spec.ts`

3 of 8 citations: 2 partial, 1 smoke.

- **partial** `#coordinator-registers-a-substitution-on-behalf-of-an-absent-handler`  
  coordinator admin exposes a bulk-reassign action with a mandatory preview  
  Asserts the coordinator console offers Bulk reassign and a Preview affected work button, which is a different requirement from registering a substitution on behalf of an absent handler, and the whole body sits behind a becomesVisible guard that skips the test when the heading does not appear.

- **partial** `#handler-registers-their-own-substitution`  
  substitution renders under personal settings with a register action  
  Asserts the personal settings mount point and a Register substitution button render, never submitting a registration, so nothing about the created object's absentee, substitute, status or createdBy is checked.

- **smoke** `#waarnemer-sees-substituted-work-in-my-work`  
  My Work renders without error and supports the substituted filter when present  
  Asserts only that the My Work page renders an Urgency button and no Internal Server Error text, and the substituted toggle is clicked inside an if that simply skips when the toggle is absent, so a build routing no substituted work at all stays green. Was `partial` on 2026-09-11.


### `tests/e2e/spec-coverage/queue.spec.ts`

3 of 4 citations: 2 partial, 1 smoke.

- **partial** `#an-empty-queue-says-so`  
  an empty result renders the empty state, not a bare table  
  The test drives the queue with a filter that should match nothing but its locator is an OR over the empty state and table rows, so it never proves the result was empty nor that the empty state specifically rendered.

- **partial** `#the-queue-narrows-by-case-type`  
  the case-type sidebar narrows the queue  
  The decisive assertion sits inside an if on the sidebar item count, so an instance with no case-type facet passes trivially, and the assertion itself only forbids widening, so a facet click that changes nothing is green.

- **smoke** `#the-queue-holds-unassigned-open-cases`  
  the queue page renders for a deep link  
  The test asserts only that the deep link renders a cn-page and a Queue heading; nothing about the base filter the cited scenario is about, so a queue listing every case in the instance passes it.


### `tests/e2e/spec-coverage/workflow-operations.spec.ts`

3 of 5 citations: 3 smoke.

- **smoke** `#scenario-dash-v1-006a-board-columns-reflect-status-types`  
  workflow board renders its heading and a status/empty surface  
  Asserts the board's own h2 and then a locator that matches either a status column or the no-statuses empty state, so a board rendering zero columns passes, and nothing reads a column name, the ordering Ontvangen, In behandeling, Besluitvorming or the per-column case count the scenario requires. Was `partial` on 2026-09-11.

- **smoke** `#scenario-dash-v1-006a-board-columns-reflect-status-types`  
  workflow board loads without dossiq console errors  
  Asserts that the board heading is visible and that no dossiq-origin console error appeared; it says nothing about one column per non-final status type, their order or their case counts, so the cited scenario is credited by a load check.

- **smoke** `#scenario-overview-01a-display-all-cases-on-map`  
  case map renders its heading and an interactive map surface  
  Asserts the Cases on map heading, then a surface matched by '.leaflet-container, [class*="map"]', a fallback almost any wrapper element satisfies, plus no server error and no console errors; nothing about markers or polygons per geometry type, clustering at low zoom or the auto-fit the scenario requires. Was `partial` on 2026-09-11.


### `tests/e2e/case-create-form.spec.ts`

2 of 11 citations: 2 partial.

- **partial** `#requirement-req-fcf-003-a-case-type-brings-its-own-questions`  _credits nothing_  
  files a case with its case type answers  
  Right surface, and it does catch an answer written nowhere, but it deliberately accepts EITHER store, reading created.properties before falling back to caseProperty rows, which is precisely the shape REQ-FCF-003 forbids when it says answers are never properties of the case itself; it never asserts the case was created without a dynamic key, so a build that stores every answer on the case passes. Either the test is anti-coverage for that clause or the requirement is stale since 7882afdc, and one of the two has to move. Re-anchor at #answers-are-stored-against-the-case-not-in-it.

- **partial** `#requirement-req-fcf-006-the-dialog-reads-as-a-form-not-a-schema`  _credits nothing_  
  labels a case type question in words, not as an identifier  
  Right surface, wrong requirement: every assertion here is about a definition name rendering in sentence case, and REQ-FCF-006, which the anchor lands on, is about two columns and a full-width multi-line widget, so breaking the layout leaves this test green. The claim it does prove is stated in REQ-FCF-003's prose, whose nearest scenario is #choosing-a-case-type-adds-its-questions, and no scenario anywhere states the sentence-case rule outright, so one has to be written before this citation can credit anything.


### `tests/e2e/case-list-lenses.spec.ts`

2 of 25 citations: 2 partial.

- **partial** `no anchor, cites openspec/specs/task-management/spec.md`  _credits nothing_  
  the task due windows narrow the collection, edges included  
  The citation names a file and no requirement; the test calls the openregister flow-task endpoint directly and asserts its dueAfter and dueBefore semantics, so it proves upstream behaviour while a dossiq lens that sent the wrong predicates would leave every assertion green. Was `verified` on 2026-09-11.

- **partial** `no anchor, cites openspec/specs/task-management/spec.md`  _credits nothing_  
  overdue is the instant comparison, so a task due later today is not late  
  The citation names a file and no requirement; the test asserts the engine's own overdue projection over the API and never touches the Tasks index, so a broken Overdue chip on the page under test survives it untouched. Was `verified` on 2026-09-11.


### `tests/e2e/case-parties.spec.ts`

2 of 14 citations: 2 partial.

- **partial** `#assign-a-case-to-a-team`  
  the Team facet lists the team with a count of one  
  Same test under the short citation form: the count of one is proven on the API facet payload with two marked cases seeded, but nothing asserts that any facet option renders, which is what the scenario says the reader sees. Not in the 2026-09-11 population.

- **partial** `#assign-a-case-to-a-team`  
  the Team facet lists the team with a count of one  
  Same test under the short citation form: the count of one is proven on the API facet payload with two marked cases seeded, but nothing asserts that any facet option renders, which is what the scenario says the reader sees. Not in the 2026-09-11 population.


### `tests/e2e/case-requester.spec.ts`

2 of 11 citations: 2 partial.

- **partial** `no anchor, cites openspec/specs/initiator-selection/spec.md`  _credits nothing_  
  a company requester persists as the uuid and the projection  
  The citation names no requirement, only the spec file; the test reads back the very row beforeAll seeded through the API and asserts the four fields the fixture itself wrote, a tautology that no picker, form or save-path breakage can redden, since it never opens a page and never exercises the write the requirement is about. Was `verified` on 2026-09-11.

- **partial** `no anchor, cites openspec/specs/initiator-display/spec.md`  _credits nothing_  
  the list filters on the requester name  
  The citation names no requirement, only the spec file; the narrowing is proven by a direct API query on initiatorDisplayName rather than by typing in the sidebar filter, and the only UI assertion, getByText(/^(Requester|Aanvrager)$/) over the whole page, is also satisfied by the Requester column header the sibling test asserts, so a sidebar that offers no requester filter still passes. Was `verified` on 2026-09-11.


### `tests/e2e/spec-coverage/kanban-board-keyboard-status-transition.spec.ts`

2 of 2 citations: 2 partial.

- **partial** `#scenario-dash-v1-006f-keyboard-only-status-transition-new`  
  a case card exposes a keyboard-operable "Move to…" menu  
  Opens the Move to menu from the keyboard and asserts the other seeded column is offered as a menu item, then presses Escape on purpose, so none of the scenario's three outcomes is asserted: the status write through the same persistence path, the card moving column, and the card body's own Enter/Space activation staying unaffected.

- **partial** `#scenario-dash-v1-006g-drag-path-unchanged-new`  
  case cards remain draggable for mouse/touch users  
  Asserts the seeded card carries draggable="true" and nothing else: it never dispatches a drag, never drops a card in another column and never checks the status was persisted, so a regression in the dragstart or drop handlers, which is what the scenario protects, leaves this green.


### `tests/e2e/spec-coverage/my-work.spec.ts`

2 of 2 citations: 1 partial, 1 smoke.

- **partial** `#scenario-card-and-table-view`  _spec says exclude_  
  shows the current user's assigned cases as a card list  
  Asserts the Urgency and Newest sort buttons and a Cards button are visible, never that card view is the rendered default nor that the table view shows identifier, title, case type, status and deadline.

- **smoke** `#scenario-card-and-table-view`  _spec says exclude_  
  view mode can be switched between cards and table  
  Clicks the Table toggle and then asserts only that the body does not read Internal Server Error, so the table could render nothing and the five columns the scenario names are never read.


### `tests/e2e/spec-coverage/work-navigation.spec.ts`

2 of 4 citations: 1 partial, 1 smoke.

- **partial** `no anchor, cites openspec/specs/nav-dedup-and-grouping/spec.md`  _credits nothing_  
  Contacts is a top-level domain, not a sixth work surface  
  the citation names no requirement, and the cited spec never mentions Contacts at all: its requirements are the Work group's four leaves, the Cases and Analytics group memberships and the single substitution entry, every one of which survives this test, which asserts only that one Contacts link exists and is visible without expanding a group

- **smoke** `no anchor, cites openspec/specs/my-work/spec.md`  _credits nothing_  
  the cases page stays reachable by direct link  
  the citation names no requirement, only the file; the test asserts the page shell rendered and a Cases heading is visible after a deep link, which is a page-load check and touches none of the my-work spec's assigned-case index, card display or lens requirements


### `tests/e2e/vth-inspection-result-authz.spec.ts`

2 of 5 citations: 2 partial.

- **partial** `#a-submitted-result-is-readable-back`  _spec says exclude_  
  the stored assignee may submit  
  The test posts a result but never calls GET /api/vth/cases/{id}/inspection-results, so nothing in it can fail if a submitted result is not readable back. Not in the 2026-09-11 population.

- **partial** `#the-cases-assignee-submits-a-result`  _safety, spec says exclude_  
  the stored assignee may submit  
  Asserts the stored assignee's submission is not a 403, which catches a guard that refuses the handler, but never checks that an inspectionResult was stored with this case and completedBy, and a 500 would satisfy not.toBe(403) just as well. Not in the 2026-09-11 population.


### `tests/e2e/app-chrome.spec.ts`

1 of 4 citations: 1 partial.

- **partial** `#a-reader-can-date-the-claim`  
  FeaturesRoadmapView compares dossiq and states the comparison limits  
  The only date check is toContainText('2026') over the whole comparison, which the added-rows sentence ('the most recent of them on 10 September 2026') already satisfies, so dropping comparedOn entirely stays green and the 'in their own language' clause is asserted nowhere. Was `verified` on 2026-09-11.


### `tests/e2e/case-detail-kpis-and-tabs.spec.ts`

1 of 4 citations: 1 partial.

- **partial** `no anchor, cites openspec/specs/ncvue-w2-leaves-adoption/spec.md`  _credits nothing_  
  the sidebar Notes tab renders the notes leaf, not an unknown element  
  The citation names no requirement, only the spec file, and none of that file's five requirements (saved views, multi-column sort, mention notification, version-history tab, notifier subjects) is asserted; the test proves only that the Notes tab mounts CnNotesTab with its composer, and a conditional test.skip on a missing tab button means the tab vanishing reports as a skip rather than a failure.


### `tests/e2e/case-documents.spec.ts`

1 of 2 citations: 1 partial.

- **partial** `#scenario-req-zak-006b-restore-is-disabled-for-definitief-documents`  _safety_  
  Versions on a row opens the panel, and restore is refused on a final document  
  The disabled check runs inside a loop over restore.count(), so an instance whose version panel lists no versions passes with no assertion executed, and the scenario's other halves, both versions listed and the hover message, are deliberately not asserted; the only unconditional line is the fixture read that the document is final.


### `tests/e2e/case-header.spec.ts`

1 of 6 citations: 1 partial.

- **partial** `#the-number-reads-under-the-title`  
  the case number, type, status and assignee read under the title  
  The scenario says the SUBTITLE under the title reads the identifier, which the requirement ties to config.subtitleField; the test reads case-header-identifier, a dd inside CaseHeaderRow.vue, so dropping subtitleField from the manifest leaves every assertion here green, and the page title itself is never asserted either. Was `verified` on 2026-09-11.


### `tests/e2e/case-hours-leaf.spec.ts`

1 of 5 citations: 1 partial.

- **partial** `#the-leaf-reads-the-right-case`  _credits nothing_  
  booking hours through the dialog raises the headline  
  Books through the dialog and asserts the headline rose by the booked hours, but write and read both derive the same case literal, so a leaf filtering on the WRONG domainObjectRef stays green; it never asserts domainObjectType is dossiq:case or that the literal came from the page config.


### `tests/e2e/case-types-tabs.spec.ts`

1 of 2 citations: 1 partial.

- **partial** `#empty-case-type-list`  
  admin settings surface has an add-control for case types  
  Never establishes the scenario's empty register and asserts neither the empty-state message nor the guidance text, only an add control that renders identically on a populated list. Not in the 2026-09-11 population.


### `tests/e2e/dashboard-tiles.spec.ts`

1 of 9 citations: 1 partial.

- **partial** `#scenario-you-complete-a-task-from-the-row`  _spec says exclude_  
  a My work row opens the task, which is where Pick up and Complete are  
  the cited scenario now carries an @e2e exclude saying row actions are blocked upstream, and this test contradicts it while asserting only that clicking a My work row navigates to a task URL, never that Complete removes the task from the table or drops the case's task count Was `smoke` on 2026-09-11.


### `tests/e2e/spec-coverage/admin-settings.spec.ts`

1 of 4 citations: 1 partial.

- **partial** `#empty-case-type-list`  
  case type list renders its management surface and add control  
  Its own comment concedes the list body is data-dependent, so it asserts the heading and the add control instead; the empty state message and the guidance sentence the scenario names are never read.


### `tests/e2e/spec-coverage/avg-verwerkingenlogging.spec.ts`

1 of 3 citations: 1 partial.

- **partial** `#scenario-procest-hosts-no-processing-activities-page`  _credits nothing_  
  procest no longer hosts a processing-activities page  
  Drives the retired route but asserts only that one English heading is absent, never reading the manifest for the navigation entry, the VerwerkingenOverview registration or the integrations gating; the manifest's AvgRegisterLink carries no section key at all, so the scenario's third THEN is already false in the source while this test stays green, and the view file no longer exists so the heading could not render either way. Was `verified` on 2026-09-11.


### `tests/e2e/spec-coverage/brp-kvk-initiator.spec.ts`

1 of 7 citations: 1 partial.

- **partial** `#contacts-source-degrades-gracefully`  
  contact tab shows an explicit empty state, never an error toast  
  Drives the Contact tab and asserts the empty state plus the absence of any error toast, but it never establishes the scenario's GIVEN: /contactsmenu/contacts is core and answers 200 with no matches on the runner, so deleting the try/catch in searchContacts that implements the degrade would not redden this test. Was `verified` on 2026-09-11.


### `tests/e2e/spec-coverage/doorlooptijd-dashboard.spec.ts`

1 of 2 citations: 1 smoke.

- **smoke** `#doorlooptijd-page-renders-heading`  
  renders the processing time page heading on navigation  
  asserts only that a level-2 heading reading Processing time is visible and that the body carries no Internal Server Error, and the scenario names the heading Processing Time Analytics, which the page no longer renders, so the spec text is stale and the test proves only that the page loaded Was `verified` on 2026-09-11.


### `tests/e2e/spec-coverage/related-case-linking.spec.ts`

1 of 1 citations: 1 partial.

- **partial** `#section-lists-relations-with-navigation`  _spec says exclude_  
  the Related cases panel lists a relation  
  opens whatever case happens to be first in the list and asserts the related-cases section is visible and has stopped saying Loading, never that any relation is listed with the target's title and status; two test.skip escapes can also retire it silently, and the scenario itself carries an @e2e exclude line whose reason names this very file


### `tests/e2e/spec-coverage/store-surface.spec.ts`

1 of 3 citations: 1 partial.

- **partial** `no anchor, cites openspec/specs/dossiq-store-surface/spec.md`  _credits nothing_  
  the store page is reachable by direct link  
  Anchorless, and the spec states no route or deep-link requirement: the test asserts the store-page container renders at /store but never the built-in templates or the not_configured outcome, so it survives a store surface that renders an error inside that container.


### `tests/e2e/spec-coverage/task-management.spec.ts`

1 of 1 citations: 1 partial.

- **partial** `#view-the-global-task-list`  
  global task list page renders its rows and columns, and offers no add button  
  The test proves the list renders its seeded row, offers no Add button and carries a Subject header, but never reads the status, assignee, due date or priority cells the cited scenario says every row must show.


### `tests/e2e/spec-coverage/ui-pages.spec.ts`

1 of 3 citations: 1 partial.

- **partial** `#dashboard-page-renders-heading-and-widget-grid`  
  dashboard renders the manifest widget grid shell  
  Asserts the app content is visible and the dashboard grid container is attached, explicitly accepting an empty container, plus no Internal Server Error; it never asserts the level-2 Dashboard heading the scenario names, nor a single widget tile or unavailable-placeholder shell, so a dashboard rendering an empty grid and no heading passes.

