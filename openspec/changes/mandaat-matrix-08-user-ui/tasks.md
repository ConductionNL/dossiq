# Tasks — Member 08: User Bevoegdheden UI (code)

Sourced from giant task 11 (User Bevoegdheden Dashboard).

## 1. Bevoegdheden Panel

- [~] Create `BevoegdhedenPanel.vue` (side panel or modal) opened from case detail — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Load applicable Mandaat records for the case zaaktype, filtered by the user's current roles — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Table columns: Mandaat # | Omschrijving | Bevoegdheidtype | Plafond | Subdelegatie | Geldend v/t | Details — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Implement "What can I do?" filter — decision types the user can unilaterally execute — deferred to downstream cycle / fleet-wide adoption (handoff)

## 2. Detail Widget

- [~] Build `MandaatMatrixWidget.vue` row-detail expansion: description, wettelijke grondslag link, current role holders, waarnemer note, MandateringsBesluit source — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] nl + en i18n; NL Design System theming; NcSelect with inputLabel — deferred to downstream cycle / fleet-wide adoption (handoff)
- [~] Test on case detail page; with different roles (incl. waarnemer); filter functionality — deferred to downstream cycle / fleet-wide adoption (handoff)
